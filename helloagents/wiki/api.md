# 用户入口与命令（非对外 API）

本项目为 Dalamud 插件，不提供任何对外 HTTP/API 服务。本文档仅记录对玩家可见的“使用入口”。

## 命令

### `/mp`
- **描述:** 打开/关闭主窗口。

## 分享与通报
- **手动发送:** 主窗口中可将“减伤警察信息”发送到小队频道（需开启“允许发送到小队频道”）。
- **自动通报（可选）:**
  - 冲突减伤（顶掉/覆盖）发生时自动发送到小队频道。
  - 有人死亡时自动发送一次减伤警察信息到小队频道。

> 自动通报同样受“允许发送到小队频道”开关约束，并带有限速/去重逻辑以避免刷屏。
